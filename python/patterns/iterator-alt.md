---
id: "python-patterns-iterator-alt"
title: "Iterator Alt"
language: "python"
category: "patterns"
tags: ["iterator-alt", "python", "patterns"]
version: "3.10+"
retrieval_hint: "Iterator Alt python patterns design pattern"
last_verified: "2026-05-25"
confidence: "medium"
---

# Iterator Alt

> **Autogenerated from:** `references/python-patterns`

## When to Use
- When you need to decouple iterator alt implementation from client code
- When the iterator alt logic varies independently from the rest of the system
- When you want to enforce a consistent iterator alt interface across implementations
- When the iterator alt behavior needs to be configured or swapped at runtime
- When standardizing iterator alt handling across a team or codebase

## Standard Pattern

```python
"""
Implementation of the iterator pattern using the iterator protocol from Python

*TL;DR
Traverses a container and accesses the container's elements.
"""

from __future__ import annotations


class NumberWords:
    """Counts by word numbers, up to a maximum of five"""

    _WORD_MAP = (
        "one",
        "two",
        "three",
        "four",
        "five",
    )

    def __init__(self, start: int, stop: int) -> None:
        self.start = start
        self.stop = stop

    def __iter__(self) -> NumberWords:  # this makes the class an Iterable
        return self

    def __next__(self) -> str:  # this makes the class an Iterator
        if self.start > self.stop or self.start > len(self._WORD_MAP):
            raise StopIteration
        current = self.start
        self.start += 1
        return self._WORD_MAP[current - 1]


# Test the iterator


def main():
# ... (truncated for skeleton)
```

## Common Mistakes

```python
# WRONG: Tight coupling to concrete implementations
# Direct instantiation/usage of specific classes without abstraction layer

# CORRECT: Programming to an interface
# Use an abstraction layer that allows swapping implementations

# WRONG: Violating Single Responsibility
# The iterator alt pattern handling mixed with unrelated business logic

# CORRECT: Separation of concerns
# Keep iterator alt logic in dedicated, focused components

# WRONG: Inconsistent error handling
# Different callers handle iterator alt errors differently

# CORRECT: Centralized error handling
# Use a uniform error handling strategy through the iterator alt abstraction
```


## Gotchas
- Over-applying iterator alt when a simpler solution (direct call, if/else) suffices
- Thread safety: iterator alt implementations often assume single-threaded access
- Testing complexity: mock/stub setup for iterator alt abstractions can be verbose

## Related
- python/stdlib/pathlib.md

