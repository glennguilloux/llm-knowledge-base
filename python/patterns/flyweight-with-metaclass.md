---
id: "python-patterns-flyweight-with-metaclass"
title: "Flyweight With Metaclass"
language: "python"
category: "patterns"
tags: ["flyweight-with-metaclass", "python", "patterns"]
version: "3.10+"
retrieval_hint: "Flyweight With Metaclass python patterns design pattern"
last_verified: "2026-05-25"
confidence: "medium"
---

# Flyweight With Metaclass

> **Autogenerated from:** `references/python-patterns`

## When to Use
- When you need to decouple flyweight with metaclass implementation from client code
- When the flyweight with metaclass logic varies independently from the rest of the system
- When you want to enforce a consistent flyweight with metaclass interface across implementations
- When the flyweight with metaclass behavior needs to be configured or swapped at runtime
- When standardizing flyweight with metaclass handling across a team or codebase

## Standard Pattern

```python
import weakref


class FlyweightMeta(type):
    def __new__(mcs, name, parents, dct):
        """
        Set up object pool

        :param name: class name
        :param parents: class parents
        :param dct: dict: includes class attributes, class methods,
        static methods, etc
        :return: new class
        """
        dct["pool"] = weakref.WeakValueDictionary()
        return super().__new__(mcs, name, parents, dct)

    @staticmethod
    def _serialize_params(cls, *args, **kwargs):
        """
        Serialize input parameters to a key.
        Simple implementation is just to serialize it as a string
        """
        args_list = list(map(str, args))
        args_list.extend([str(kwargs), cls.__name__])
        key = "".join(args_list)
        return key

    def __call__(cls, *args, **kwargs):
        key = FlyweightMeta._serialize_params(cls, *args, **kwargs)
        pool = getattr(cls, "pool", {})

        instance = pool.get(key)
        if instance is None:
            instance = super().__call__(*args, **kwargs)
            pool[key] = instance
        return instance


class Card2(metaclass=FlyweightMeta):
# ... (truncated for skeleton)
```

## Common Mistakes

```python
# WRONG: Tight coupling to concrete implementations
# Direct instantiation/usage of specific classes without abstraction layer

# CORRECT: Programming to an interface
# Use an abstraction layer that allows swapping implementations

# WRONG: Violating Single Responsibility
# The flyweight with metaclass pattern handling mixed with unrelated business logic

# CORRECT: Separation of concerns
# Keep flyweight with metaclass logic in dedicated, focused components

# WRONG: Inconsistent error handling
# Different callers handle flyweight with metaclass errors differently

# CORRECT: Centralized error handling
# Use a uniform error handling strategy through the flyweight with metaclass abstraction
```


## Gotchas
- Over-applying flyweight with metaclass when a simpler solution (direct call, if/else) suffices
- Thread safety: flyweight with metaclass implementations often assume single-threaded access
- Testing complexity: mock/stub setup for flyweight with metaclass abstractions can be verbose

## Related
- python/stdlib/pathlib.md

