---
id: "go-patterns-generator"
title: "Generator Pattern (yield, coroutine)"
language: "go"
category: "patterns"
tags: ["generator", "go-generator", "yield", "pattern-yield", "channel-yield", "channel-generator", "coroutine", "lazy-sequence", "go", "patterns"]
version: "1.21+"
retrieval_hint: "Go generator pattern yield channel coroutine lazy sequence channel-yield design pattern"
last_verified: "2026-05-25"
confidence: "medium"
---

# Generator

> **Autogenerated from:** `references/go-patterns/concurrency`

## When to Use
- When you need to decouple generator implementation from client code
- When the generator logic varies independently from the rest of the system
- When you want to enforce a consistent generator interface across implementations
- When the generator behavior needs to be configured or swapped at runtime
- When standardizing generator handling across a team or codebase

## Standard Pattern

```go
func Count(start int, end int) chan int {
    ch := make(chan int)

    go func(ch chan int) {
        for i := start; i <= end ; i++ {
            // Blocks on the operation
            ch <- i
        }

		close(ch)
	}(ch)

	return ch
}


fmt.Println("No bottles of beer on the wall")

for i := range Count(1, 99) {
    fmt.Println("Pass it around, put one up,", i, "bottles of beer on the wall")
    // Pass it around, put one up, 1 bottles of beer on the wall
    // Pass it around, put one up, 2 bottles of beer on the wall
    // ...
    // Pass it around, put one up, 99 bottles of beer on the wall
}

fmt.Println(100, "bottles of beer on the wall")

```

## Common Mistakes

```go
# WRONG: Tight coupling to concrete implementations
# Direct instantiation/usage of specific classes without abstraction layer

# CORRECT: Programming to an interface
# Use an abstraction layer that allows swapping implementations

# WRONG: Violating Single Responsibility
# The generator pattern handling mixed with unrelated business logic

# CORRECT: Separation of concerns
# Keep generator logic in dedicated, focused components

# WRONG: Inconsistent error handling
# Different callers handle generator errors differently

# CORRECT: Centralized error handling
# Use a uniform error handling strategy through the generator abstraction
```


## Gotchas
- Over-applying generator when a simpler solution (direct call, if/else) suffices
- Thread safety: generator implementations often assume single-threaded access
- Testing complexity: mock/stub setup for generator abstractions can be verbose

## Related
- go/stdlib/error-handling.md

- go/stdlib/channels.md
