---
id: "go-patterns-publish-subscribe"
title: "Publish Subscribe"
language: "go"
category: "patterns"
tags: ["publish-subscribe", "go", "patterns"]
version: "1.21+"
retrieval_hint: "Publish Subscribe go patterns design pattern"
last_verified: "2026-05-25"
confidence: "medium"
---

# Publish Subscribe

> **Autogenerated from:** `references/go-patterns/messaging`

## When to Use
- When you need to decouple publish subscribe implementation from client code
- When the publish subscribe logic varies independently from the rest of the system
- When you want to enforce a consistent publish subscribe interface across implementations
- When the publish subscribe behavior needs to be configured or swapped at runtime
- When standardizing publish subscribe handling across a team or codebase

## Standard Pattern

```go
type Message struct {
    // Contents
}


type Subscription struct {
	ch chan<- Message

	Inbox chan Message
}

func (s *Subscription) Publish(msg Message) error {
	if _, ok := <-s.ch; !ok {
		return errors.New("Topic has been closed")
	}

	s.ch <- msg

	return nil
}


type Topic struct {
	Subscribers    []Session
	MessageHistory []Message
}

func (t *Topic) Subscribe(uid uint64) (Subscription, error) {
    // Get session and create one if it's the first

    // Add session to the Topic & MessageHistory

    // Create a subscription
}

func (t *Topic) Unsubscribe(Subscription) error {
	// Implementation
}

func (t *Topic) Delete() error {
# ... (truncated for skeleton)
```

## Common Mistakes

```go
# WRONG: Tight coupling to concrete implementations
# Direct instantiation/usage of specific classes without abstraction layer

# CORRECT: Programming to an interface
# Use an abstraction layer that allows swapping implementations

# WRONG: Violating Single Responsibility
# The publish subscribe pattern handling mixed with unrelated business logic

# CORRECT: Separation of concerns
# Keep publish subscribe logic in dedicated, focused components

# WRONG: Inconsistent error handling
# Different callers handle publish subscribe errors differently

# CORRECT: Centralized error handling
# Use a uniform error handling strategy through the publish subscribe abstraction
```


## Gotchas
- Over-applying publish subscribe when a simpler solution (direct call, if/else) suffices
- Thread safety: publish subscribe implementations often assume single-threaded access
- Testing complexity: mock/stub setup for publish subscribe abstractions can be verbose

## Related
- go/stdlib/error-handling.md

